licenses(["permissive"])

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "yaml_cpp_build",
    srcs = glob(["src/**/*.cpp"]),
    hdrs = glob([
        "include/**/*.h",
        "src/**/*.h",
    ]),
    includes = ["include"],
    linkstatic = 1,
)
